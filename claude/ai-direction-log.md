# AI Direction Log

| # | Date | Prompt | AI Output | Decision |
|---|------|--------|-----------|----------|
| 1 | 2026-03-29 | "Build me a very quick test page with a single button action that keeps a record of clicks and celebrates each click" | Multi-stage escalating shame/celebration page with particles, blinking badge, and quest-accept flow | Kept. Confirmed end-to-end pipeline worked. Creative execution was on target. |
| 2 | 2026-04-01 | Build the three-zone hero select screen per the Design Intent — character thumbnails left, full hero display center, stats panel right | Grid layout with thumbnail grid, hero display with enter animation, stats panel with stat bars, character data file, selection states, opacity transitions, and ready button | Kept. Matched design intent. Iterative refinements followed for sizing, spacing, and visual polish. |
| 3 | 2026-04-06 | "Now I am going to give you images of each character's emblem. When a character is selected, display that character's emblem above the character name on the right side character descriptor section. The emblem should be positioned above the character name by 8px and centered on the same horizontal axis as the name and descriptor." | Emblem images added to `public/emblems/`, emblem field added to character data, emblem rendered above char-name in StatsPanel with per-character CSS size and spacing overrides | Kept after iterative per-character tuning of size and margin-bottom offsets. |
| 4 | 2026-04-06 | "Now I want you to add a glowing elliptical character base beneath the selected hero character. It should sit directly beneath the feet, centered to the hero, and use colors that match the existing teal/cyan language of the UI." | Three-layer teal pedestal: outer bloom div, visible rim border with box-shadow, bright inner oval surface | Rejected and removed entirely. Platform intersected character shoes regardless of repositioning. Too faint after clipping fix. Still unsatisfactory after glow boost. Feature scrapped. |
| 5 | 2026-04-06 | "Add a soft glow behind the selected character so they feel more highlighted and visually important on the screen. The glow should sit behind the hero character in the center area and act like a subtle selection aura, helping separate the character from the dark background without overpowering the interface." | Diffuse radial body aura centered behind the character | Rejected — too body-shaped. Replaced via follow-up prompt with a vertical spotlight column and floor glow. Kept. |
| 6 | 2026-04-06 | "The pedestal ellipse is being clipped at the front. Remove any overflow or masking that is cutting off the front edge, and render the base as a full visible ellipse beneath the character. Keep it behind the PNG and below the shoes but make sure the front arc of the platform is fully visible." | Removed `overflow: hidden` from `.zone-center` — attempted twice outright, and once as a structural refactor moving glow elements outside `.hero-base-wrap` with selective clipping | All three attempts rejected. Each broke hero scaling or stage layout. Resolved by lifting floor glow 30px and reducing blur 28px → 8px to keep bleed within container bounds. |
| 7 | 2026-04-06 | "I want you to blend it into the existing background as a subtle atmospheric layer rather than replacing the current background outright. I want the image detail to be faintly readable so reduce the brightness, saturation, and contrast of the photo scene so it sits farther back visually. Make sure the background does not compete with the interface." | `body::before` pseudo-element with `brightness(0.32) saturate(0.45) contrast(0.85) opacity(0.72)`. `#root` set to `z-index: 1` | Kept. Snowy mountain night scene blends into the dark UI without competing with teal/cyan interface elements. |
| 8 | 2026-04-07 | "I want to create a character specific background system where each character has their own unique background image that appears when that character is selected. The transition between backgrounds should feel smooth and cinematic." | `Background.jsx` component stacking all character bg layers as fixed `div`s; active layer crossfades to `opacity: 0.72` over 600ms; null and kevin both map to `bg.png` | Kept. Per-character bg images sourced by user for Rae, Hanna, Daniel. Kevin kept original. |
| 9 | 2026-04-07 | "Add a subtle fog or mist layer along the bottom of the screen that enhances the atmospheric depth of the scene. It should feel like ground-level mist creeping in from the edges rather than a flat overlay." | `Fog.jsx` + `Fog.css`: three-layer gradient fog pinned to `bottom: 0`, height 415px, `z-index: 2`, drift animation at 28s/20s/14s alternating | Kept after darkening, rounding fix, and height increase. Fog removed from default/unselected state per user direction. |
| 10 | 2026-04-07 | "Add a top-right utility UI group containing coins, settings, and notifications. The coin element should show the player's current balance. The settings and notifications should be icon buttons." | `UtilityBar.jsx` fixed top-right with boxed coin currency, stroke notification bell, stroke gear settings | Initial version kept. Iterative refinements followed for unboxing, icon style, sizing, and layout. |
| 11 | 2026-04-07 | "Can the currency not be boxed too. Can the icon for the currency have that same teal gradient color used throughout the UI and be shaped like a 3D coin with a dollar sign in the middle." | 3D SVG coin using `linearGradient` with `#016858 → #24AEA8`, edge ellipse for depth, highlight, `$` text; all three elements unboxed | Kept. Currency number set to teal `#24AEA8`, bold. Multiple sizing iterations followed. |
| 12 | 2026-04-07 | "Add a profile icon to the top-right utility group while preserving the existing alignment, spacing logic, and padding that are already established. I want the new profile element to feel like it belongs to the same top-right utility row without disrupting the current layout. The green active button/indicator should appear as a small status dot attached to the profile icon." | Circular avatar `img` 27px with `border-radius: 50%`, green `#4ade80` status dot `position: absolute` bottom-right with dark border | Kept. Live clock added to right of profile. Controls cluster grouped with 26px internal gap, clock offset 72px. |
| 13 | 2026-04-07 | "Add a bottom-left disclaimer / game fact text block anchored to the bottom-left corner of the screen. It should follow the same left padding logic as the Back button but be positioned at the bottom instead of the top. The label such as DISCLAIMER: should be in ALL CAPS using fauna one medium and set to 24px. the supporting body text should use Fauna One Regular set to 18px. The transition between messages should feel smooth and understated." | `Disclaimer.jsx` fixed `bottom: 48px left: 48px`, 5 rotating messages, 500ms opacity fade every 6s, Fauna One font, label + body layout | Kept. Sizes reduced to 16px/14px. Body forced to single line with `white-space: nowrap`. Opacity muted to 0.55/0.38. |
| 14 | 2026-04-07 | "Scale down the full right-side character information group by 0.1 while preserving the exact same spacing, alignment, and padding rules that already exist. I want the whole group to become slightly smaller overall but I do not want the internal hierarchy or structure to change." | `transform: scale(0.9)` on `.stats-panel` with `transform-origin: top center` | Kept. Additional 0.05x applied as second pass → `scale(0.85)`. `.char-identity` given separate `scale(0.95)`. |
